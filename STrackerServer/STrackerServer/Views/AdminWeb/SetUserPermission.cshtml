@model STrackerServer.Models.Admin.SetPermissionView

@{
    ViewBag.Title = "Set Permission";
    Layout = "~/Views/Shared/MainLayout.cshtml";
}

<div class="container-fluid">
    <div class="row-fluid">
        <div class="span3">
            <div class="box">
                <div style="text-align: center">
                    <a href="@Url.Action("Show","UsersWeb", new { id = Model.Id })">
                       <img class="img-rounded" src="@Model.PictureUrl" alt="No Artwork"/> 
                    </a>
                </div> 
                <br/>
                <div style="text-align: center">
                    <a href="@Url.Action("Show","UsersWeb", new { id = Model.Id })">@Model.Name</a>
                </div>
            </div>
        </div>

        <div class="span6">
            <div class="box">
                <h2>Permission</h2> 
            </div>
        </div>
    </div>
</div>
