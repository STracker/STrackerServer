@using STrackerServer.BusinessLayer.Permissions
@model STrackerServer.Models.Admin.SetPermissionView

@{
    ViewBag.Title = "Permission";
    Layout = "~/Views/Shared/MainLayout.cshtml";
}

<div class="container-fluid">
    <div class="row-fluid">
        <div class="span3">
            <div class="box">
                <div style="text-align: center">
                    <a href="@Url.Action("Show","UsersWeb", new { id = Model.Id })">
                       <img class="img-rounded" src="@Model.PictureUrl" alt="No Artwork"/> 
                    </a>
                </div> 
                <br/>
                <div style="text-align: center">
                    <a class="btn btn-block btn-primary" href="@Url.Action("Show","UsersWeb", new { id = Model.Id })">@Model.Name</a>
                </div>
            </div>
        </div>

        <div class="span6">
            <div class="box" style="text-align: center">
                <h2>Permission</h2>
            </div>
            
            <div class="box">
                <br/>
                <p><b>Permission: </b>@Model.PermissionName</p>
                <br/>
                @Html.ValidationSummary()
                @using(Html.BeginForm())
                {
                    @Html.HiddenFor(view => view.Id)

                    foreach(var permission in Model.Permissions.Keys)
                    {
                        <label class="radio">
                            @if(Model.Permission == (int)permission)
                            {
                                <input type="radio" checked="checked" name="permission" value="@Model.Permissions[permission]" /><span>@Enum.GetName(typeof(Permission), permission)</span>
                            }else
                            {
                                <input type="radio" name="permission" value="@Model.Permissions[permission]" /><span>@Enum.GetName(typeof(Permission), permission)</span>
                            }
                        </label>
                    }
                    <br/>
                    <input class="btn btn-primary" type="submit" value="Set Permission"/>
                }
            </div>
        </div>
    </div>
</div>
