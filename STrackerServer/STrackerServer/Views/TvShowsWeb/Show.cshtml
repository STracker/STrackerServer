@model STrackerServer.Models.TvShow.TvShowView

@{
    ViewBag.Title = @Model.Name;
    Layout = "~/Views/Shared/TvShowLayout.cshtml";
}

<div class="container-fluid">
    <div class="row-fluid">
        <div class="span3">
            <img class="img-rounded" src="@Model.Poster" alt="No Artwork"/>  

            <div class="extra-padding"></div> 
            @if (User.Identity.IsAuthenticated)
            {
                using (Html.BeginForm("Subscribe", "TvShowsWeb", new { tvshowId = Model.TvShowId }))
                {
                    @Html.Hidden("RedirectUrl", Url.Action("Show","TvShowsWeb", new { tvshowId = Model.TvShowId }))

                    if (Model.IsSubscribed)
                    {
                        @Html.Hidden("isSubscribing", false)
                        <input class="btn btn-block btn-danger" type="submit" value="Unsubscribe"/>
                    }else
                    {
                        @Html.Hidden("isSubscribing", true)
                        <input class="btn btn-block btn-success" type="submit" value="Subscribe"/>
                    }  
                }  
            }else
            { 
                <a class="btn btn-block btn-success" href="@Url.Action("Login","Account", new { returnUrl =  Url.Action("Show","TvShowsWeb", new { tvshowId = Model.TvShowId })})">Subscribe</a>
                <div class="extra-padding"></div> 
            }
            
            <p>
                <a class="btn btn-block btn-primary" href="@Url.Action("Rate","TvShowsWeb", new { tvshowId = Model.TvShowId })">Rate</a>
            </p> 
            <p>
                <a class="btn btn-block btn-primary" href="@Url.Action("Suggest","TvShowsWeb", new { tvshowId = Model.TvShowId })">Suggest</a>
            </p>  
            <p>
                <a class="btn btn-block btn-primary" href="@Url.Action("Comments","TvShowsWeb", new { tvshowId = Model.TvShowId })">Comments</a>
            </p>
        </div> 
        <div class="span6">
            <div class="box">
                <h2>@Model.Name</h2>
                <br/>
                <p>@Model.Description</p> 
                <p>
                    <b>Rating: </b>
                    @if (Model.Rating != 0)
                    {
                        <span>@Model.Rating/5 (from @Model.RatingsCount Users)</span>
                    }else
                    {
                        <span>Not Rated</span>
                    }
                </p>
                @if (User.Identity.IsAuthenticated && Model.UserRating != -1)
                {
                    <p><b>Your Rating: </b> @Model.UserRating/5</p>
                }

                <p><b>Air Day: </b> @Model.AirDay</p>
                <p><b>Runtime: </b> @Model.Runtime min</p>              
                <p><a href="http://www.imdb.com/title/@Model.TvShowId">IMDB</a></p>  

                <p><b>Genre:</b></p>
                <ul>
                    @foreach(var genre in Model.Genres)
                    {
                        <li>
                            <a href="@Url.Action("Show","GenreWeb", new { name = genre.Id })">@genre.Id</a>
                        </li>            
                    }
                </ul>
            </div>     
            <div class="box">
                <b> Seasons:</b>
                
                @foreach (var season in this.Model.SeasonSynopses)
                {
                    <a class="season" data-api="@Url.HttpRouteUrl("api_season_get", new { tvshowId = season.TvShowId, number = season.SeasonNumber})" href="@Url.Action("Show", "SeasonsWeb", new { tvshowId = season.TvShowId, seasonNumber = season.SeasonNumber })">@season.SeasonNumber</a><b> | </b>
                }
            </div>
            <div id="display-season" class="box hide">
                <div style="text-align: right">
                    <button class="btn-link" id="display-hide">Close</button>
                </div>         
                <br/>
                <table id="display-table" class="table table-condensed">

                </table>
            </div> 
            <div class="box">
                <div style="display: inline">
                    <div style="display: inline-block">
                        <h3>Cast</h3>
                    </div>
                    <div id="cast-button" class="hide" style="display: inline-block">
                        <button class="btn-link">(Open)</button>
                    </div> 
                </div>
                <br/>              
                <table id="cast-table" class="table table-condensed">
                    <tr>
                        <th>Photo</th>
                        <th>Name</th>
                        <th>Character</th>
                    </tr>
        
                    @foreach(var actor in Model.Actors)
                    {
                        <tr>
                            <td>
                                @if(actor.Photo != null)
                                {
                                    <img class="icon-actor" src="@actor.Photo" alt="No Artwork"/>
                                }
                                else
                                {
                                    <img class="icon-actor" src="https://dl.dropboxusercontent.com/u/2696848/default-profile-pic.jpg" alt="No Artwork"/>
                                }
                            </td>
                            <td>
                                @actor.Name
                            </td>
                            <td>
                                @actor.CharacterName
                            </td>
                        </tr>  
                    }
                </table>
            </div>     
        </div>
        <div class="span3">
            <div class="box">
                <h2 style="text-align: center">Next Episodes</h2>
                <br/>
                <div class="accordion" id="accordion">
                    @foreach(var episode in Model.NewEpisodes)
                    {
                        <dl>              
                            <dt>@episode.Date</dt>
                            <dd><a href="@episode.Uri">@episode.Name</a></dd>           
                        </dl>       
                    }
                </div>
            </div>
        </div>
    </div>
</div>