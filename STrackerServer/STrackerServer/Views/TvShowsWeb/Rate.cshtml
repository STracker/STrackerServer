@model STrackerServer.Models.TvShow.TvShowRating

@{
    ViewBag.Title = "Rate";
    Layout = "~/Views/Shared/MainLayout.cshtml";
}

<div class="container-fluid">
    <div class="row-fluid">
        <div class="span3">
            <img class="img-rounded" src="@Model.Poster" alt="No Artwork"/>  

            <div class="extra-padding"></div> 

            <p>
                <a class="btn btn-block btn-primary" href="@Url.Action("Show","TvShowsWeb", new { tvshowId = Model.TvShowId })">@Model.TvShowName</a>
            </p> 
        </div> 
        <div class="span6">
            <div class="box">
                <h2>@Model.TvShowName</h2> 
                <br/>
                <p><b>Rating: </b> @Model.Rating/5 (from @Model.RatingsCount Users)</p>               

                @Html.ValidationSummary()
                @using (Html.BeginForm("Rate", "TvShowsWeb"))
                {
                    @Html.HiddenFor(rating => rating.TvShowId)
                    <div class="form-inline" style="display: inline">
                        <div style="display: inline-block">
                            <b>Your Rating: </b>
                        </div>
                        <div style="display: inline-block">
                            @Html.EditorFor(rating => rating.Value)
                        </div>
                        <div style="display: inline-block">
                            <input class="btn btn-danger" type="submit" value="Rate"/>
                        </div>
                    </div>  
                }  
            </div>
        </div>
    </div>
</div>