@model dynamic

@{
    ViewBag.Title = "Api Documentation";
    Layout = "~/Views/Shared/MainLayout.cshtml";
}

<div class="container-fluid">
    		<div class="row-fluid">
    			<h1>STracker API</h1>
    		</div>
    		<div class="row-fluid">
	    		<div class="span12 box-whiter">
	    			STracker provides a public API to access his resources by other applications. The communication is made through <abbr title="HyperText Transfer Protocol" class="initialism">HTTP</abbr> protocol, where the representations of the resources are returned in JSON or XML. The return format depends on HTTP Header Accept. 
	        	</div>
        	</div>
        	<div class="row-fluid">
	        	<div class="span12 box-whiter">
	        		This API is based on <abbr title="Representational State Transfer" class="initialism">REST</abbr> architectural style, where all URI's are hypertext driven. The methods of the STracker API are:
	        		<dl>
						<dt>
							<span class="label label-info">GET</span>
		        			<span><code>/api/tvshows?name={tvshowName}&start={startNumber}&end={endNumber}</code></span>
		        		</dt>
						<dd>
							Returns a list of television show synopses based on the argument "name" passed on query string.
						    The parameters "start" and "end" define the range of the returned list, these parameters are optional. Parameter start begins in index zero.
                        </dd>
                        <br/>
                        <dt>
                            <span class="label label-info">GET</span>
                            <span><code>/api/tvshows/{tvshowId}</code></span>
                        </dt>
						<dd>
							Returns a television show representation based on his IMDB ID.
						</dd>
						<br/>
						<dt>
							<span class="label label-info">GET</span>
		        			<span><code>/api/tvshows/{tvshowId}/seasons</code></span>
		        		</dt>
						<dd>
							Returns a list of seasons synopses of a television show.
						</dd>
						<br/>
						<dt>
							<span class="label label-info">GET</span>
		        			<span><code>/api/tvshows/{tvshowId}/seasons/{seasonNumber}</code></span>
		        		</dt>
						<dd>
							Returns a season representation.
						</dd>
						<br/>
						<dt>
							<span class="label label-info">GET</span>
		        			<span><code>/api/tvshows/{tvshowId}/seasons/{seasonNumber}/episodes</code></span>
		        		</dt>
						<dd>
							Returns a list of episode synopses based on the season of the television show.
						</dd>
						<br/>
						<dt>
							<span class="label label-info">GET</span>
		        			<span><code>/api/tvshows/{tvshowId}/seasons/{seasonNumber}/episodes/{episodeNumber}</code></span>
		        		</dt>
						<dd>
							Returns a representation of an episode of a television show.
						</dd>
						<br/>
						<dt>
							<span class="label label-info">GET</span>
		        			<span><code>/api/genres</code></span>
		        		</dt>
                        <dd>
                            Returns a list of genres synopses.
                        </dd>
                        <br/>
						<dt>
							<span class="label label-info">GET</span>
		        			<span><code>/api/genres/{genreName}</code></span>
		        		</dt>
                        <dd>
                            Returns a list of television show synopses based on the genre passed in the URI.
                        </dd>
                        <br/>
						<dt>
							<span class="label label-info">GET</span>
		        			<span><code>/api/tvshows/{tvshowId}/newepisodes?date=date</code></span>
		        		</dt>
						<dd>
							Returns a list of episodes synopses of the newest episodes from one television show.
						</dd>
					</dl>
	        	</div>
        	</div>
    	</div>